@using BusnLogicLaag
@model List<Outfit>

@{
    ViewData["Title"] = "OutfitsTonenGebr";
}

<h1>Mijn Outfits</h1>

<p>
<a asp-action="OutfitAanmaken">Nieuw</a>
</p>
<table class="table">
  <thead>
    <tr>
     <th>
       Outfit
     </th>
     <th>
       Categorie
     </th>
     <th>
       Prijs
     </th>
     <th>
       Titel
     </th>
    <th></th>
   </tr>
  </thead>
 <tbody>

@foreach (Outfit item in Model) {
  <tr>
   <td>
    <img src="~/Images/@item.FileAdress" class="rounded" width="70" height="70" asp-append-version="true" />
   </td>
   <td>
    @Html.DisplayFor(modelItem => item.DeCategory)
   </td>
   <td>
    @Html.DisplayFor(modelItem => item.Prijs)
   </td>
   <td>
    @Html.DisplayFor(modelItem => item.Titel)
   </td>
   <td>
    <div class="btn-group">
      <a asp-controller="Outfit" asp-action="OutfitUpdaten" asp-route-id="@item.ID" class="btn btn-primary">Updaten</a>
      <a asp-controller="Outfit" asp-action="OutfitVerwijderen" asp-route-id="@item.ID" class="btn btn-sm btn-danger m1-1">Verwijderen</a>
    </div>
   </td>
  </tr>
}
 </tbody>
</table>
